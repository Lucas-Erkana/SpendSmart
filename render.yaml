databases:
  - name: spendsmart
    databaseName: spendsmart_30b0
    user: spendsmart_user

services:
  - type: web
    name: spend-smart
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: spendsmart
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false